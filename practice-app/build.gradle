tasks.named('bootJar') {
    enabled = true
}

tasks.named('jar') {
    enabled = false
}

dependencies {

    // Module dependency
    runtimeOnly project(":practice-jpa")
    implementation project(":practice-websocket")
    implementation project(":practice-domain")
    implementation project(":practice-websocket")

    // Web
    implementation 'org.springframework.boot:spring-boot-starter-web'
}
